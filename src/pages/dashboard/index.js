import React from 'react';
import LayoutDashboard from '../../components/DashboardLayout';

const index = () => (
  <div>
    <LayoutDashboard>Main Dashboard</LayoutDashboard>
  </div>
);

export default index;
